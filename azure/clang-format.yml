trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: sudo apt-get install clang-format
  displayName: 'install'

- script: clang-format --style=Google src/*.cpp src/*.h
  displayName: 'build'
